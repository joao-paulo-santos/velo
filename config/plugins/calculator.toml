name = "calculator"
display_label = "Calculator"
display_prefix = ""
context_name = "Calculator"
type = "preview"
global = true
teleport = true
eval_cmd = "qalc '{input}'"
# copy_cmd = "wl-copy"  # default; Enter copies the preview to the clipboard
