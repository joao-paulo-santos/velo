name = "wifi-connect"
display_label = "Connect"
type = "select"
list_cmd = "bash -c '~/.config/hypr-tofi/plugins/wifi/list.sh'"
format = "json"
label_field = "name"
value_field = "id"
as = "network"
template = "nmcli device wifi connect '{network}' password '{password}'"
next = "enter-password"
